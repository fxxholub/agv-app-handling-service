version: '3.9'

services:
  webapi:
    image: leuze_agv_handling_service.webapi
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - "8080"
      - "8181"
    ports:
      - "8080:8080"
      - "8081:8081"
    env_file:
      - .env
